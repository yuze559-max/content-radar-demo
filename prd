# Content Radar｜内容雷达 产品需求文档（PRD）

> 版本：V1.0  
> 对应原型：Content Radar V0.9.3  
> 日期：2026-08-21  
> 在线 Demo：https://content-radar-demo-xi.vercel.app  
> 项目仓库：https://github.com/yuze559-max/content-radar-demo

| 版本 | 日期 | 状态 | 说明 |
|---|---|---|---|
| V1.0 | 2026-08-21 | 评审稿 | 基于 V0.9.3 Demo 整理产品需求、AI 边界与后续 Harness Agent 合规执行规划 |

## 1. 产品需求解读

### 1.1 产品定位

Content Radar 是面向金融科技内容运营人员的 AI 辅助工作台，将“每日热点发现”和“对标账号学习”合并为一条可解释、可审核、可复盘的内容生产链路。

产品不追求让 AI 自动运营账号，而是帮助运营人员完成以下工作：

1. 从公开来源中发现值得跟进的金融科技热点；
2. 从对标账号中学习高表现内容的选题、Hook、结构和互动机制；
3. 生成带来源、风险标记和相似度提示的候选内容；
4. 由人工完成事实、合规和表达审核后再导出；
5. 根据人工反馈和发布后表现持续调整推荐策略。

### 1.2 目标用户与核心问题

| 用户角色 | 当前问题 | 产品提供的价值 |
|---|---|---|
| 金融科技内容运营 | 热点分散、核验耗时、选题容易重复 | 聚合热点、去重排序、生成可追溯候选 |
| 内容负责人 | 难以统一内容标准与审核口径 | 统一风险规则、审核状态和发布边界 |
| 合规/审核人员 | AI 结论来源不清、执行过程不可追踪 | 查看证据、风险命中、修改记录和执行轨迹 |

### 1.3 MVP 范围

**本期包含：**热点池、账号观点、7 天内容计划、候选生成、审核清单、任务记忆、策略迭代、系统设置和高保真方向预览。

**本期不包含：**自动发布、自动交易、个性化投资建议、未经人工确认的策略生效，以及真实 X API 的生产级接入。

### 1.4 目标指标

以下为后续试点目标，并非当前 Demo 已实现结果：

| 目标 | 建议指标 |
|---|---|
| 提高选题效率 | 从热点进入审核清单的平均处理时长 |
| 提高证据完整性 | 候选内容双来源覆盖率、来源链接可访问率 |
| 控制内容风险 | 高风险内容拦截率、无依据确认率 |
| 提高审核效率 | 候选一次通过率、平均修改次数 |
| 验证策略学习 | 预测互动率与实际互动率偏差、策略调整采纳率 |

## 2. 工作流设计

```mermaid
flowchart LR
    A[公开新闻/RSS/X 数据] --> B[采集与标准化]
    B --> C[事件去重与多源验证]
    C --> D[优先级与风险评分]
    D --> E{是否值得跟进}
    E -->|否| F[归档/观望]
    E -->|是| G[结合账号画像生成候选]
    G --> H[事实/来源/相似度/风险检查]
    H --> I[人工审核]
    I -->|驳回| J[记录原因并调整规则]
    I -->|修改| G
    I -->|确认| K[导出或进入发布排期]
    K --> L[回收 24h/7d 互动数据]
    L --> M[任务记忆与策略迭代]
    M --> G
```

候选内容状态为：`待审核 → 已确认 / 已驳回`；修改后的候选重新进入待审核。系统只允许导出已确认内容，不执行自动发布。

## 3. 功能与 UI 理念

### 3.1 功能模块

| 模块 | 核心功能 | 优先级 |
|---|---|---|
| 今日工作台 | 任务概览、7 天计划、Top 3 优先内容、数据日历 | P0 |
| 每日热点 | 事件聚类、评分、风险分级、证据查看、候选生成 | P0 |
| 对标账号观点 | 账号画像、推文样例、可借鉴机制、差异化计划 | P0 |
| 审核清单 | 查看依据、修改、确认、驳回、批量处理 | P0 |
| 任务记忆与优化 | 调度状态、30 天记忆、人工反馈和策略迭代 | P1 |
| 系统设置 | 账号定位、数据源、风险词和相似度阈值 | P1 |
| 高保真预览 | 企业后台与内容运营两种视觉方向 | P2 |

### 3.2 UI 设计理念

- 采用 Codex 工作台式布局：左侧导航、顶部任务状态、主区块卡片化呈现；
- 黑白低保真优先表达信息架构，不依赖装饰传递功能；
- 每条候选同时展示“内容、证据、风险、操作”，减少跨页核对；
- 风险信息贴近操作按钮，高风险状态不可直接确认；
- “策略迭代”和“近 30 天内容记忆”并列展示，分别解释 AI 如何调整、AI 已经学到什么；
- 桌面端双栏提升运营效率，窄屏自动改为单栏，保留核心审核操作。

## 4. AI 使用方式

| 环节 | AI/LLM 职责 | 规则或系统职责 | 人工职责 |
|---|---|---|---|
| 数据采集 | 不负责直接抓取 | RSS/API/页面解析、时间戳与 URL 去重 | 配置合法数据源 |
| 事件去重 | 辅助判断语义相似事件 | 主体、事件类型、时间窗指纹 | 处理边界案例 |
| 跟进判断 | 根据账号定位生成跟进理由 | 赛道、时效、风险和来源评分 | 决定是否跟进 |
| 候选生成 | 生成摘要、角度、Hook 和中英文候选 | 注入来源、风险规则与账号定位 | 复核并修改 |
| 相似度检查 | 计算语义相似度 | 字符相似度、阈值和冷却规则 | 高相似内容必须改写 |
| 账号画像 | 提炼选题、结构和互动机制 | 筛选高表现样本 | 确认可借鉴与不可照搬部分 |
| 策略复盘 | 总结预测与实际表现差异 | 汇总人工动作和互动数据 | 确认策略是否生效 |

AI 输出必须包含结构化字段：`candidate_id、topic、summary、hook、sources、risk_level、risk_reason、similarity_score、model_version、prompt_version、trace_id`。缺少必要来源或风险字段时，候选不得进入“已确认”。

## 5. 人工审核点

以下节点必须保留人工责任，不能由 Agent 自动越过：

1. **来源核验：**重要事实原则上需要两个独立来源，检查标题、时间、主体和原文链接；
2. **数字核验：**金额、比例、时间和机构名称必须与原始证据一致；
3. **金融合规：**收益承诺、个股买卖建议、夸大确定性等表达必须拦截；
4. **政治与敏感议题：**高风险内容默认不进入发布流程；
5. **相似度审核：**与对标内容相似度达到高风险阈值时必须人工改写；
6. **策略更新：**人工采用、驳回和修改记录可形成建议，但修改全局规则前必须审批；
7. **最终发布：**系统仅导出已确认内容，账号发布责任由人工承担。

## 6. 当前局限

- 当前为静态可交互 Demo，部分热点与任务状态使用演示数据；
- X 账号数据主要来自快照和样例，尚未完成官方 API 的连续采集；
- 多源验证仍有人工补源环节，不能保证所有事件达到三源覆盖；
- 策略迭代展示了闭环机制，但尚未实际自动更新线上推荐权重；
- 尚未建立正式的离线评测集、灰度机制和生产监控；
- 当前权限模型较轻，未覆盖企业级 RBAC、密钥隔离和审计归档；
- LLM 仍可能产生事实偏差、错误归因或不合适的金融表达，因此不能取消人工审核。

## 7. 后续优化安排

### 7.1 阶段一：真实数据与可追溯性

- 接入 X API、财联社、金十数据、Bloomberg RSS、万得等合规数据源；
- 建立事件、证据、候选、审核动作和发布结果的数据模型；
- 为每次生成保存模型、Prompt、证据快照、规则版本和 trace_id；
- 增加接口失败重试、来源降级、重复任务幂等与数据过期规则。

### 7.2 阶段二：评测与反馈学习

- 建立事实准确、来源完整、风险识别、差异化和语言质量评测集；
- 回收发布后 24 小时和 7 天的赞、转发、评论、浏览量；
- 对 Hook、内容结构和发布时间进行 A/B 测试；
- 策略调整先生成建议，经人工确认后灰度生效，并保留回滚版本。

### 7.3 阶段三：引入 Harness Agent 受控执行层

本文将 **Harness Agent** 定义为“受约束的 Agent 执行层”，用于高风险、强监管的金融内容任务；该能力属于后续规划，并非当前 Demo 已实现功能。

```mermaid
flowchart TD
    A[任务请求] --> B[任务契约校验]
    B --> C{身份/权限/数据范围是否合规}
    C -->|否| D[拒绝执行并记录原因]
    C -->|是| E[受控工具白名单]
    E --> F[沙箱内生成或读取]
    F --> G[事实/风险/输出 Schema 校验]
    G -->|失败| H[停止、降级或人工接管]
    G -->|通过| I{是否涉及外部写入或高风险结论}
    I -->|是| J[人工审批]
    I -->|否| K[输出候选]
    J -->|批准| K
    J -->|拒绝| H
    K --> L[保存完整审计轨迹]
```

Harness Agent 的产品约束建议如下：

| 约束能力 | 产品要求 |
|---|---|
| 任务契约 | 每个任务声明目标、输入、允许输出、截止条件和责任人；超出范围立即停止 |
| 最小权限 | 工具采用白名单；采集 Agent 不具备发布权限，生成 Agent 不可修改合规规则 |
| 路径约束 | 固定执行顺序：取证 → 校验 → 生成 → 风险检查 → 人工审批 → 导出 |
| 输出约束 | 使用统一 JSON Schema；关键字段缺失时不得进入下一节点 |
| 风险策略 | 低风险可生成候选；中风险强制补证并审核；高风险默认拦截 |
| 人工关卡 | 外部写入、规则修改、高风险结论和最终发布必须由指定角色审批 |
| 审计留痕 | 保存输入、证据、工具调用、模型/Prompt/规则版本、人工动作和最终输出 |
| 可靠执行 | 写操作使用幂等键；瞬时失败可重试，参数错误和 401/403 立即停止 |
| 版本与回滚 | 策略、Prompt 和规则均可版本化；异常时回滚到上一稳定版本 |

建议验收目标：未授权工具调用在测试集中应被全部阻断；缺失来源的候选不能确认；所有外部写入均需人工审批并保留 trace_id；相同幂等键不得产生重复写入。

### 7.4 阶段四：企业化运营

- 接入 SSO/RBAC，区分运营、审核、合规和管理员权限；
- 增加审计报表、异常告警、成本监控和模型降级；
- 将已验证的策略按 1%–5% 流量灰度，观察安全、体验、成本后再扩大；
- 若引入自动发布，仅限低风险、预先批准的模板和账号，并保留人工紧急停止能力。

## 8. 版本规划

| 版本 | 主要范围 | 成功标准 | 状态 |
|---|---|---|---|
| V0.9.3 | 可交互 Demo、完整页面与人工审核闭环 | 可演示、交互无报错 | 已完成 |
| V1.0 | 真实数据接入、候选与审核数据持久化 | 核心链路可在测试环境运行 | 规划中 |
| V1.1 | 评测集、互动回收、策略建议与灰度 | 策略变化可评估、可回滚 | 规划中 |
| V2.0 | Harness Agent、RBAC、审计与受控执行 | 权限、路径、输出和人工关卡可验证 | 远期规划 |

## 9. 验收标准与待确认项

### 9.1 MVP 验收标准

- 每条进入审核清单的候选均展示来源、风险等级和相似度状态；
- 高风险候选无法直接确认，驳回和修改动作均有记录；
- 已确认内容可以导出，但系统不会自动发布；
- 任务记忆可展示近 30 天主题，并向新候选提供重复或风险提示；
- 桌面端与移动端核心页面无横向溢出，关键操作可完成。

### 9.2 待确认项

1. **[必须确认]** 首批真实数据源及其授权方式；建议先接入公开 RSS，再评估 X API 与万得授权成本。
2. **[必须确认]** 企业环境中的审核角色和最终责任人；建议至少区分运营与合规审核。
3. **[建议确认]** 高、中、低风险规则阈值；建议通过不少于 100 条人工标注样本校准。
4. **[建议确认]** Harness Agent 首批允许执行的任务；建议从“只读取证 + 候选生成”开始，不开放自动发布。
5. **[可后续补充]** 互动率、打开率和采纳率的正式口径及统计窗口。

